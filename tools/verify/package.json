{
  "name": "aio-quality-profiles-verify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Verification harness that runs the REAL AIOStreams stream-expression evaluator against this repo's .expressions.json profiles.",
  "scripts": {
    "build": "tsc",
    "verify": "npm run build && node verify.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bytes": "^3.1.2",
    "expr-eval": "^2.0.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bytes": "^3.1.4",
    "typescript": "^5.6.3"
  }
}